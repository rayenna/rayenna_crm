# Bin — docs pending deletion

Files here were moved during a **June 2026 documentation housekeeping** pass. They are obsolete, duplicated, or superseded by docs under [`docs/`](../docs/README.md).

**Review this folder**, then delete the whole `Bin/` directory when you are satisfied nothing important was lost.

## Contents

| Folder | What was moved |
|--------|----------------|
| `root-scratch/` | Old root-level `.md` files (deploy checklists, local dev recovery, password-reset notes, Windows/PowerShell setup, etc.) |
| `proposal-engine/` | PE roadmaps, phase logs, deprecated backend/shared notices, Sentry notes |
| `client/` | One-off UI suggestion doc |
| `prompts/` | Cursor / test prompts |
| `misc/` | Loose `.txt` notes |
| `legacy-help/` | Unused `help-docs/docs/` trees (not loaded by CRM Help — live help uses section folders like `getting-started/`, `zenith/`, etc.) |

## Safe to delete?

Generally **yes**, if you rely on:

- [`docs/`](../docs/README.md) for engineering docs
- `client/public/help-docs/` for in-app CRM Help
- `src/content/consumer-help/` for Solar Hub help

If you need a historical PE roadmap, check `proposal-engine/MODERNIZATION_PROGRESS.md` and `ai-roof-layout-2d-roadmap.md` here before deleting.
