/** Pixel height for Recharts donut containers in Zenith (segment cards, payment radar, etc.). */
export const ZENITH_DONUT_CHART_HEIGHT_PX = 252

/** Mobile / narrow: extra vertical room so the pie ring is not cut off by the slot or overflow rules. */
export const ZENITH_DONUT_CHART_HEIGHT_MOBILE_PX = 308
